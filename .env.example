# Required: your Google Gemini API key.
# This is only used server-side (Next.js API routes) — never exposed to the browser.
# Get your key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here
